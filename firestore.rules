        rules_version = '2';
        service cloud.firestore {
          match /databases/{database}/documents {
            // TEMPORARY: ALLOW ALL ACCESS FOR ADMIN INITIALIZATION
            match /{document=**} {
              allow read, write: if request.auth != null;
            }
          }
        }